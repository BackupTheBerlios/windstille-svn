(windstille-tiles
  (tiles (id 1)
        (image "../data/images/virtualreality.png")
        (colmap 1))

  (tiles (id 64)
        (image "../data/images/virtualreality-background.png")
        (colmap 0))
)

;; EOF ;;
